import { cloneDeep, isEqual, minBy, orderBy } from 'lodash-es';

export const deepCloneUtil = cloneDeep;
export const orderByUtil = orderBy;
export const minByUtil = minBy;
export const deepEqualUtil = isEqual;
